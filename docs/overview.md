# Обзор CRM Kit

## Продукт

CRM Kit — облачная CRM/ERP-платформа для сервисного бизнеса.
Первый целевой рынок — **фитнес-клубы**; архитектура рассчитана на расширение в салоны, клиники, SPA, образовательные центры и другой сервисный бизнес.

## Что уже работает

- **Multi-tenant SaaS** — компании, филиалы, роли пользователей
- **Клиенты** — список, поиск, CRUD, привязка к абонементам
- **CRM** — задачи, **канбан сделок с настраиваемыми воронками**
- **Расписание** — события и занятия
- **Frontend** — dashboard в стиле Bitrix24, авторизация, профиль
- **API** — REST с token-auth и изоляцией по `?company=`

## Архитектурный подход

- модульный монолит (Django + Next.js)
- чёткие границы между доменными модулями
- явная конфигурация по окружениям
- локальная разработка через Docker (PostgreSQL) + venv
- операционный CRM — во frontend; `/admin/` — панель платформы

## Карта модулей backend

```
core          — базовые классы, healthcheck
accounts      — пользователи, membership, auth
companies     — tenant (компания)
branches      — филиалы
clients       — клиенты фитнес-клуба
crm           — задачи, сделки, воронки (канбан)
schedule      — расписание
memberships   — абонементы (в разработке)
```

Подробнее о CRM: [architecture/crm-module.md](./architecture/crm-module.md).

## Карта документации

**Главный указатель:** [docs/README.md](./README.md)

| Раздел | Путь |
|--------|------|
| Этапы (stages) | `docs/stages/` |
| Уроки | `docs/lessons/` |
| API | `docs/api/` |
| Архитектура | `docs/architecture/` |
| Roadmap | `docs/roadmap.md` |
| Handoff | `handoff.md` |

## Зачем такая структура

Продукт рассчитан на долгий рост. Кодовая база должна оставаться понятной новичку и достаточной для коммерческого масштаба. В любой момент должно быть ясно:

1. Что делает этот файл?
2. Зачем он существует?
3. Как безопасно его изменить?

Учебный контур (уроки 13–21) отвечает на вопрос **как добавлять новые фичи** по образцу уже существующих.

## С чего начать новому разработчику

1. [Урок 06 — локальный запуск](./lessons/06-how-to-run-local-project.md)
2. [Урок 13 — цепочка разработки](./lessons/13-how-to-develop-full-feature.md)
3. [Урок 21 — канбан и воронки](./lessons/21-real-example-fitness-kanban-pipelines.md) — актуальный сложный пример
