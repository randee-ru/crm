# Scanner IP blocklist for nginx.
# Format (one entry per line):
#   1.2.3.4 1;
#   2001:db8::1 1;
#
# After editing on the server, reload nginx:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod exec -T nginx nginx -s reload
#
# Seeded from known Host/UA probes against the VPS.

# Generic WordPress / CMS scanners seen with Host: sport-max.com.ru
34.181.254.84 1;
